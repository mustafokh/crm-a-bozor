# Railway FAQAT Nixpacks ishlatadi — root'da Dockerfile yo'q
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "sh scripts/railway-start.sh"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
