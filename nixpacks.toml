# Railway Nixpacks — Dockerfile ishlatilmaydi
[phases.setup]
nixPkgs = ["nodejs_20", "openssl"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "sh scripts/railway-start.sh"
